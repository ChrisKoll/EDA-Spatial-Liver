```{python}
# | label: load-libraries
# | echo: false
# | include: false

from datetime import datetime
import os
import requests

import anndata as ad
import matplotlib.pyplot as plt
import numpy as np
import pandas as pd
import scipy.sparse as sp
import seaborn as sns
```

```{python}
#| label: run-information
#| echo: false

# Input directory
data_dir = "../data/training_data"

# Output directories
run_dir = f"run_{datetime.now().strftime('%Y-%m-%d_%H-%M')}"
out_dir = f"../output/training_data_integration/{run_dir}"

# Ensure the output directory exists
os.makedirs(out_dir, exist_ok=True)

print(f"📂 Input Seurat objects from: {data_dir}\n💾 Outputs will be saved in: {out_dir}")
```

## Load Data

```{python}
# | label: load-data-StSt

# Load steady state data
StSt_data_file = os.path.join(data_dir, "countTable_mouseStSt.h5ad")
adata_stst = ad.read_h5ad(StSt_data_file, backed=True)

adata_stst
```

```{python}
#| label: load-data-NAFLD

# Load NAFLD data
NAFDL_data_file = os.path.join(data_dir, "countTable_mouseNafld.h5ad")
adata_nafld = ad.read_h5ad(NAFDL_data_file)

adata_nafld
```

Look at counts.

```{python}
# Counts of first cell
set(adata_nafld.X[1, :].todense().flatten().tolist()[0])
```

- Counts look unnormalized
- Would expect deciaml numbers
- Probably raw counts

```{python}
# Counts of first cell
set(adata_stst.X[1, :].todense().flatten().tolist()[0])
```
